"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getNoAliasPath = void 0;
const path_1 = __importDefault(require("path"));
const os_1 = __importDefault(require("os"));
function getNoAliasPath(sourcePath, importPath, matcher) {
    let formatSourcePath = sourcePath;
    let formatImportPath = importPath;
    let result = matcher(formatImportPath, undefined, undefined, [
        '.ts',
        '.tsx',
        '.js',
        '.jsx',
    ]);
    if (!result) {
        return null;
    }
    formatImportPath = result;
    if (os_1.default.platform() === 'win32') {
        formatImportPath = formatImportPath.replace(/\\/g, '/');
        formatSourcePath = formatSourcePath.replace(/\\/g, '/');
    }
    try {
        const packagePath = require.resolve(importPath, {
            paths: [process.cwd(), ...module.paths],
        });
        if (packagePath) {
            return importPath;
        }
    }
    catch (_a) { }
    const resolvedPath = path_1.default.posix.relative(path_1.default.dirname(formatSourcePath), formatImportPath) || './';
    return resolvedPath[0] === '.' ? resolvedPath : './' + resolvedPath;
}
exports.getNoAliasPath = getNoAliasPath;
